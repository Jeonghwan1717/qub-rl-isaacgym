# QUB 이족보행 강화학습 — 좌우 대칭 보행 확보

**요약 한 줄** — 리워드를 감으로 조정하는 대신 매 단계 측정으로 표적을 특정했고, 그 과정에서
보행을 막고 있던 **config·URDF 수준의 구조적 버그 4개**를 찾아 고쳤다. 좌우 대칭 잔차는
99% 이상 감소했고 양발 모두 발바닥 전체로 접지한다.

---

## 1. 문제

QUB 13-DOF 이족보행 정책이 전진 보행을 학습하지 못했다. 증상이 단계적으로 바뀌었다.

| 단계 | 증상 |
|---|---|
| 초기 | 제자리에 서 있는 것이 최적 — 발을 떼지 않음 |
| 중기 | 걷기는 하나 절뚝임. 한 다리가 더 바깥으로 돌아감 |
| 후기 | 좌우 관절은 대칭인데 한쪽 발이 뒤꿈치·모서리로만 접지 |

세 번 모두 처음에는 리워드 문제로 보였으나, **실제 원인은 매번 다른 곳에 있었다.**

---

## 2. 접근 — 측정으로 표적을 특정한다

리워드 스케일을 조정하고 결과를 보는 방식은 이 프로젝트에서 반복적으로 실패했다.
대신 다음 절차를 따랐다.

1. 증상을 **정량 지표로 정의**한다 (예: "절뚝인다" → 좌우 발의 몸 기준 앞뒤 위치 차이 cm)
2. 그 지표를 **직접 측정**한다 (`legged_gym/scripts/diag/`)
3. 측정값이 리워드 부족 때문인지, 다른 원인 때문인지 **구분**한다
4. 리워드를 만들 때는 **실측 오차로 스케일을 계산**한다

핵심은 3번이다. 네 번 중 세 번은 리워드가 아니라 config 버그였다.

### 측정에서 지킨 원칙

- **말단(발)을 잰다.** 관절각은 운동학적 잉여로 상쇄될 수 있다. 발목 진폭이 작다고 판단했다가
  틀린 적이 있는데, 발 자체를 재니 발 피치 범위는 오히려 3배 컸다.
- **기준 프레임을 맞춘다.** "발바닥이 땅에 닿는가"는 월드 기준 질문이다. 골반 기준 pitch가
  −1.32°(평평해 보임)일 때 땅 기준 기울기는 9.93°였다.
- **DC와 AC를 나눈다.** 보행은 좌우 180° 위상차라 매 순간 좌우가 다른 것이 정상이다.
  문제는 한쪽으로 치우쳐 고정된 성분(DC)이다.

---

## 3. 찾아낸 구조적 버그 4개

### 3.1 종료 조건 링크명 오매칭

```python
terminate_after_contacts_on = ["base", "abad", "hip", "knee"]
```

실제 URDF 링크명은 `thigh` / `calf`라 `abad`와 `knee`는 **아무것도 매칭하지 않았고**,
부분 문자열 매칭이라 `hip`이 **hip 링크 4개를 전부** 잡았다.

→ 힙에 접촉만 나면 즉시 에피소드 종료. **발을 떼는 것 자체가 위험한 행동**이 되어,
세 세션 동안 "가만히 서 있는 게 최적"이던 원인이었다.

### 3.2 soft joint limit이 관절 범위의 중점 기준

`_process_dof_props`는 soft limit을 관절 범위의 **중점** 기준으로 계산한다.
QUB의 hip_roll은 범위가 `R [−2.4, 0]` / `L [0, 2.4]`로 극단적으로 치우쳐 있어
중점이 ∓1.2 rad(∓69°)라는 엉뚱한 곳에 온다.

→ 기본자세(∓0.03)조차 위반이 되어, **R 관절 하나가 `dof_pos_limits` 전체 페널티의 89%**를
차지했다. 이 항은 양다리를 6.9° 이상 벌리라고 요구하는데, 그렇게 하면 발 간격이 0.41 m가 되어
`feet_distance_max`(0.35)에 더 크게 걸린다.

| 선택지 | dof_pos_limits | feet_distance_max | 합 |
|---|---|---|---|
| 한쪽만 벌림 | 0.12 | 0 | **0.12** ← 최적 |
| 양쪽 대칭 벌림 | 0 | 0.18 | 0.18 |

→ **"한쪽만 벌리고 다른 쪽은 페널티를 그냥 내는 것"이 비용상 최적해**였고, 그것이 절뚝임의 실체였다.
대칭 리워드를 아무리 더해도 이 충돌 위에 요구를 하나 더 얹는 것에 불과했다.

### 3.3 ankle_pitch 좌우 축이 반대인데 기본값이 동일

12개 다리 관절 중 **ankle_pitch만** 좌우 축이 반대다 (`R (0,−1,0)` / `L (0,+1,0)`).
모든 관절 origin이 `rpy="0 0 0"`이라 URDF의 축이 곧 월드 축이므로, 같은 관절값이
**정반대 물리 회전**을 만든다. 그런데 config는 좌우 모두 `+0.1`이었다.

정책도 리워드도 개입시키지 않고 액션 0으로만 세운 기본자세 측정:

| | 발 피치 |
|---|---|
| 왼발 | +11.99° |
| 오른발 | +1.44° |
| **차이** | **10.55°** |

→ **로봇이 가만히 서 있는 자세부터 좌우가 어긋나 있었다.** `posture`가 이 비대칭 자세를
계속 목표로 삼아 대칭 리워드와 정면으로 싸웠고, 6개 관절 중 ankle_pitch만 유일하게 악화됐다.
`L_ankle_pitch`를 `−0.1`로 고쳐 **0.06°**가 됐다.

### 3.4 리워드 크기 계산식 오류 (분석 측 실수)

legged_gym 로그의 `rew_<name>`은 **`raw × scale`의 시간평균**이다
(`episode_sums += raw × scale × dt`를 `max_episode_length_s`로 나누므로 dt와 스텝 수가 상쇄).
여기에 50(스텝/초)을 더 곱해 스케일을 산정했다.

→ 대칭 항들이 의도의 **1/50 크기**로 작동했고, "리워드를 넣었는데 안 듣는다"는 잘못된 결론에
도달했다. 검산으로 확인: `step_symmetry` 로그 −0.0027 / scale −2.0 → 역산 3.67 cm,
직접 측정 **3.69 cm** 일치.

---

## 4. 리워드 설계

### 원칙

| 원칙 | 근거 |
|---|---|
| **순간값이 아니라 EMA를 비교한다** | 보행은 좌우 180° 위상차라 순간 비교는 양발이 함께 움직이는 제자리뛰기를 유도한다. 원본 `_reward_symmetry`가 꺼져 있던 이유이기도 하다. 시정수 2초(≈3.4 보행주기) |
| **관절별 항을 만들지 않고 하나로 묶는다** | hip_yaw·hip_roll 전용 항을 차례로 넣었더니 각각 자기 관절은 잡았지만 다른 관절이 무너졌다. 6쌍 제곱합은 가장 나쁜 관절로 압력을 자동 재배분한다 |
| **미러 부호는 URDF 축에서 유도한다** | 시상면 반사에서 각속도는 유사벡터라 `(ωx,ωy,ωz) → (−ωx,+ωy,−ωz)`. pitch는 부호 유지, roll/yaw는 반전, 좌우 축이 반대면 한 번 더 반전 |
| **잘 되는 쪽은 수식에서 아예 뺀다** | `(L−R)²`는 좋은 쪽을 나쁘게 만들어도 만족된다. 목표를 절대값(0)이나 측정 상수로 두면 그 경로가 구조적으로 사라진다 |
| **땅에 관한 문제는 월드 기준으로 잰다** | 골반 기준 pitch를 −7.88° → −1.32°로 교정하고 성공으로 판단했으나, 같은 시점 땅 기준 기울기는 9.93°였다 |

### 주요 항

| 항 | 스케일 | 표적 |
|---|---|---|
| `joint_symmetry` | −20 | 다리 6관절쌍 미러 잔차 제곱합 (2초 EMA) |
| `left_foot_flat_contact` | −15 | 접촉 중 왼발 발바닥의 땅 기준 기울기 |
| `right_foot_flat_contact` | −50 | 접촉 중 오른발 발바닥의 땅 기준 기울기 |
| `step_symmetry` | −30 | 좌우 발의 골반 기준 앞뒤 평균 위치 차이 |
| `foot_landing_symmetry` | −30 | 착지 지점의 좌우 차이 |
| `stride_symmetry` | −30 | 보폭(이지→착지 거리)의 좌우 차이 |
| `torso_yaw_drift` | −5 | 상체 비틀림의 DC 성분만 (흔들림은 허용) |

발 접지 항의 스케일이 좌우 다른 이유: 제곱항이라 오차가 작을수록 압력이 제곱으로 작아진다.
오른발 오차(6.48°)는 왼발 시작점(9.93°)보다 작아 같은 스케일이면 압력이 1/2.3로 떨어진다.

---

## 5. 결과

동일 조건 실측 (cmd_vx 0.4 · 64 env · 도메인 랜덤화 off · 10초):

| 지표 | 09-02 시작 | **최종 (model_6000)** | 개선 |
|---|---|---|---|
| **6관절 미러 잔차 합** | 0.02672 rad² | **0.00015** | **−99.4%** |
| 최대 단일 관절 잔차 | −7.17° | **−0.50°** | −93% |
| **왼발 발바닥 기울기** | 9.93° | **0.08°** | −99.2% |
| **오른발 발바닥 기울기** | 6.48° | **0.08°** | −98.8% |
| **발 앞뒤 위치 차이** | 5.50 cm | **0.24 cm** | −96% |
| **보폭 차이** | 10.4% | **1.2%** | −88% |
| 기본자세 발 피치 비대칭 | 10.55° | **0.06°** | −99.4% |
| torso_yaw DC | −3.65° | +1.91° | — |
| 속도 추종 오차 | — | 7.1% | — |
| **생존율 (cmd 0.4)** | 64/64 | **64/64** | 유지 |

**양발이 각각 0.08°로 발바닥 전체 접지**하며, **6개 관절 잔차가 전부 0.5° 이내**다.
보행 품질 손실 없이(생존 64/64) 달성했다.

경과별 추이는 [`POLICY_PROGRESS.md`](POLICY_PROGRESS.md), 최종 상세 측정은
[`RESULTS.md`](RESULTS.md) 참고.

---

## 6. 확인된 한계와 다음 단계

### 측정으로 확인한 잔여 항목

| 항목 | 현재 | 비고 |
|---|---|---|
| **뒤꿈치 충돌 여유** | 최소 **4.23 cm** | hip_yaw가 좌우 19.4°씩 벌어져 발끝이 바깥을 향하면 뒤꿈치가 안쪽으로 모인다. 현재 충돌하지 않으나 여유가 크지 않다. `feet_distance_max`는 발 **중심** 거리만 보므로 이 위험을 감지하지 못한다 |
| **torso_yaw DC** | ±2~4° 진동 | 체크포인트마다 부호가 바뀌며 0으로 수렴하지 않는다 |
| **hip_yaw 벌어짐** | 좌우 19.4° | 대칭성·충돌·보행 품질 모두 정상이나 각도 자체가 크다. 균형에 실제로 필요한지 미확인 |

### 다음 단계

| 항목 | 난이도 | 필요 작업 |
|---|---|---|
| 외력 견고성 | 낮음 | `push_robots = True` (10초 간격, 최대 1.0 m/s). 현재 체크포인트에서 이어서 학습 가능 |
| 물리 파라미터 랜덤화 | 낮음 | 마찰 [0.2, 1.25] · 질량 [−1, +3] kg · 모터토크 ±15% · 지연 0–20 ms — 범위가 이미 정의된 채 꺼져 있다. sim-to-real 필수 |
| 거친 지형 | **높음** | `mesh_type = "plane"` → `"trimesh"`. 다만 `measure_heights = False`라 정책이 지형을 보지 못한다. actor 관측에 높이맵을 넣으면 관측 차원이 바뀌어 **재학습 필요** |
| 속도 한계 재측정 | 낮음 | 마지막 실측 0.6 m/s (09-02). 보행이 크게 바뀌었으므로 재측정 필요 |

---

## 부록 — 재현

```bash
export PATH=$HOME/miniconda3/envs/kudos/bin:$PATH
export LD_LIBRARY_PATH=$HOME/miniconda3/envs/kudos/lib:$LD_LIBRARY_PATH
export ROBOT_TYPE=QUB

# 학습
python legged_gym/scripts/train.py --task=qub_flat --headless

# 측정 (진단 스크립트 설명은 legged_gym/scripts/diag/README.md)
python legged_gym/scripts/diag/policy_report.py --task=qub_flat \
    --load_run=<run> --checkpoint=<n> --headless

# 시각 확인
python legged_gym/scripts/play.py --task=qub_flat \
    --load_run=<run> --checkpoint=<n>
```

각 리워드 항의 스케일 근거는 `legged_gym/envs/qub_flat/qub_flat_config.py` 주석에
실측값과 함께 기록되어 있다.
